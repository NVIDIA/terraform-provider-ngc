# SPDX-FileCopyrightText: Copyright (c) 2024 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: LicenseRef-NvidiaProprietary

# NVIDIA CORPORATION, its affiliates and licensors retain all intellectual
# property and proprietary rights in and to this material, related
# documentation and any modifications thereto. Any use, reproduction,
# disclosure or distribution of this material and related documentation
# without an express license agreement from NVIDIA CORPORATION or
# its affiliates is strictly prohibited.

name: CI

on:
  push:
    branches:
      - main
    tags-ignore:
      - '**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

# Prevent duplicate runs on PR and push
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: '1.24'
  GOLANGCI_LINT_VERSION: 'v2.8.0'
  GORELEASER_VERSION: 'v2.2.0'
  TEST_REPORT_FILE: report.xml

permissions:
  contents: read
  pull-requests: write
  checks: write         # Required for dorny/test-reporter
  security-events: write  # Required for SARIF upload

jobs:
  # =============================================================================
  # Stage: Test
  # =============================================================================

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9.2.0
        with:
          version: ${{ env.GOLANGCI_LINT_VERSION }}

  unit-test:
    name: Unit Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@latest

      - name: Run unit tests
        run: make test

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: |
            coverage_ut.out
            report_ut.xml
          retention-days: 30

      - name: Upload test report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Unit Test Results
          path: report_ut.xml
          reporter: java-junit
          fail-on-error: true

  # =============================================================================
  # Stage: Build
  # =============================================================================

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, unit-test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for goreleaser to work properly

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: ${{ env.GORELEASER_VERSION }}
          install-only: true

      - name: Build snapshot
        run: goreleaser release --clean --snapshot --skip=publish,announce,sign

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: dist/*.zip
          retention-days: 7

  # =============================================================================
  # Stage: Scan
  # =============================================================================

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: dist/

      - name: Run Trivy vulnerability scan
        uses: aquasecurity/trivy-action@0.33.1
        with:
          scan-type: 'fs'
          scan-ref: '.'
          vuln-type: 'os,library'
          severity: 'HIGH,CRITICAL'
          ignore-unfixed: true
          format: 'sarif'
          output: 'trivy-results.sarif'
          skip-dirs: 'vendor,node_modules,dist'

      - name: Upload Trivy SARIF report
        uses: github/codeql-action/upload-sarif@v4.31.5
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Run TruffleHog secret scan
        uses: trufflesecurity/trufflehog@v3.91.1
        with:
          path: ./
          extra_args: --only-verified

  # =============================================================================
  # Stage: Integration Test
  # =============================================================================

  integration-test:
    name: Integration Test
    runs-on: ubuntu-latest
    needs: [lint, unit-test, build, security-scan]
    environment: integration-test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install gotestsum
        run: go install gotest.tools/gotestsum@latest

      - name: Run acceptance tests
        env:
          # Secrets
          NGC_API_KEY: ${{ secrets.NGC_API_KEY }}
          # Variables - Organization
          NGC_ORG: ${{ vars.NGC_ORG }}
          NGC_TEAM: ${{ vars.NGC_TEAM }}
          NGC_ENDPOINT: ${{ vars.NGC_ENDPOINT }}
          NCA_ID: ${{ vars.NCA_ID }}
          AUTHORIZED_PARTY_1: ${{ vars.AUTHORIZED_PARTY_1 }}
          AUTHORIZED_PARTY_2: ${{ vars.AUTHORIZED_PARTY_2 }}
          # Variables - Helm
          HELM_URI: ${{ vars.HELM_URI }}
          HELM_SERVICE_NAME: ${{ vars.HELM_SERVICE_NAME }}
          HELM_SERVICE_PORT: ${{ vars.HELM_SERVICE_PORT }}
          HELM_INFERENCE_URL: ${{ vars.HELM_INFERENCE_URL }}
          HELM_HEALTH_URI: ${{ vars.HELM_HEALTH_URI }}
          HELM_VALUE_YAML_OVERWRITE: ${{ vars.HELM_VALUE_YAML_OVERWRITE }}
          HELM_VALUE_YAML_OVERWRITE_UPDATE: ${{ vars.HELM_VALUE_YAML_OVERWRITE_UPDATE }}
          # Variables - Container
          CONTAINER_URI: ${{ vars.CONTAINER_URI }}
          CONTAINER_PORT: ${{ vars.CONTAINER_PORT }}
          CONTAINER_INFERENCE_URL: ${{ vars.CONTAINER_INFERENCE_URL }}
          CONTAINER_HEALTH_URI: ${{ vars.CONTAINER_HEALTH_URI }}
          # Variables - Cluster/Backend
          BACKEND: ${{ vars.BACKEND }}
          CLUSTER_1: ${{ vars.CLUSTER_1 }}
          CLUSTER_2: ${{ vars.CLUSTER_2 }}
          CLUSTER_1_REGION: ${{ vars.CLUSTER_1_REGION }}
          CLUSTER_2_REGION: ${{ vars.CLUSTER_2_REGION }}
          INSTANCE_TYPE: ${{ vars.INSTANCE_TYPE }}
          GPU_TYPE: ${{ vars.GPU_TYPE }}
          # Variables - Model
          MODEL_1_NAME: ${{ vars.MODEL_1_NAME }}
          MODEL_1_VERSION: ${{ vars.MODEL_1_VERSION }}
          MODEL_1_URI: ${{ vars.MODEL_1_URI }}
          MODEL_1_FULLY_QUALIFIED_URI: ${{ vars.MODEL_1_FULLY_QUALIFIED_URI }}
          # Variables - Telemetry
          LOGS_TELEMETRY_ID: ${{ vars.LOGS_TELEMETRY_ID }}
          METRICS_TELEMETRY_ID: ${{ vars.METRICS_TELEMETRY_ID }}
        run: make testacc-ci

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-results
          path: |
            coverage_acc.out
            report_acc.xml
          retention-days: 30

      - name: Upload test report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Integration Test Results
          path: report_acc.xml
          reporter: java-junit
          fail-on-error: true

  # =============================================================================
  # Summary
  # =============================================================================

  ci-summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [lint, unit-test, integration-test, build, security-scan]
    if: always()
    steps:
      - name: Check job results
        run: |
          echo "## 🎯 CI Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Lint | ${{ needs.lint.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Unit Test | ${{ needs.unit-test.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Integration Test | ${{ needs.integration-test.result == 'success' && '✅ Passed' || (needs.integration-test.result == 'skipped' && '⏭️ Skipped' || '❌ Failed') }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build | ${{ needs.build.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Security Scan | ${{ needs.security-scan.result == 'success' && '✅ Passed' || (needs.security-scan.result == 'skipped' && '⏭️ Skipped' || '❌ Failed') }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📌 Commit: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "🔀 Branch: \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY

      - name: Fail if any required job failed
        if: |
          needs.lint.result == 'failure' ||
          needs.unit-test.result == 'failure' ||
          needs.integration-test.result == 'failure' ||
          needs.build.result == 'failure'
        run: exit 1
